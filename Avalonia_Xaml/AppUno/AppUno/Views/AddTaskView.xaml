<Page x:Class="AppUno.Views.AddTaskView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:AppUno.Views"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:utu="using:Uno.Toolkit.UI"
      xmlns:uen="using:Uno.Extensions.Navigation.UI"
      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid RowDefinitions="Auto,*">
        <utu:NavigationBar/>
        <StackPanel Grid.Row="1"  Margin="10">
            <TextBlock Text="New Task" FontSize="30" Margin="10" HorizontalAlignment="Center"/>
            <TextBox PlaceholderText="Task name" Text="{Binding NewTaskName, Mode=TwoWay}" Margin="8"/>
            <TextBox PlaceholderText="Description" Text="{Binding TaskDesc, Mode=TwoWay}" Margin="8"/>
            <TextBox PlaceholderText="Category" Text="{Binding TaskCatalog, Mode=TwoWay}" Margin="8"/>
            <Grid ColumnDefinitions="100,*">
                <Button Grid.Column="0"
                    Content="Add task"
                    Command="{Binding SaveTaskCommand}"
                    Padding="10" 
                    Margin="10"/>   
                <Button Grid.Column="1"
                    Content="Clear All"
                    Command="{Binding ClearCommand}"
                    Padding="10" 
                    Margin="10"/>   
            </Grid>
        </StackPanel>         
    </Grid>
</Page>
