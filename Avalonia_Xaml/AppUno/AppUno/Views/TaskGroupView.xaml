<Page x:Class="AppUno.Views.TaskGroupView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:AppUno.Views"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:utu="using:Uno.Toolkit.UI"
      xmlns:uen="using:Uno.Extensions.Navigation.UI"
      xmlns:vm="using:AppUno.ViewModels"
      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid RowDefinitions="Auto,*,Auto" Margin="10">
        <utu:NavigationBar Content="{Binding ListName}" FontSize="25">
            <utu:NavigationBar.SecondaryCommands>
                <AppBarButton Icon="Delete" Label="Delete" Command="{Binding DeleteListCommand}"/>
            </utu:NavigationBar.SecondaryCommands>
        </utu:NavigationBar>

        <StackPanel Grid.Row="1" x:Name="TaskGroupPanel">
            <ItemsControl ItemsSource="{Binding GroupedTasks}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel>
                            <!-- Category Header -->
                            <TextBlock Text="{Binding Category}" 
                                    FontSize="20"
                                    FontWeight="Bold"
                                    Padding="5"
                                    Margin="5"/>

                            <!-- Task List -->
                            <ItemsControl ItemsSource="{Binding Tasks}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel>
                                            <Button
                                                HorizontalAlignment="Stretch"
                                                HorizontalContentAlignment="Left"
                                                Background="Transparent"
                                                BorderThickness="0"
                                                Padding="5"
                                                Margin="10"
                                                Command="{utu:AncestorBinding AncestorType=UserControl, Path=DataContext.OpenTaskCommand}"
                                                CommandParameter="{Binding}">
                                                <Grid ColumnDefinitions="40,*">
                                                    <CheckBox Grid.Column="0"
                                                            IsChecked="{Binding IsDone, Mode=TwoWay}"
                                                            VerticalAlignment="Center"
                                                            Margin="10,0,10,0"/>
                                                    <TextBlock Grid.Column="1"
                                                            Text="{Binding Name}"
                                                            VerticalAlignment="Center"
                                                            FontSize="15"/>
                                                </Grid>
                                            </Button>
                                        </StackPanel>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>

        <!-- Add Task Button -->
        <Button Grid.Row="2"
                Width="50"
                Height="50"
                Margin="10"
                Padding="0"
                CornerRadius="30"
                HorizontalAlignment="Right"
                Command="{Binding AddTaskViewCommand}">
            <Image Source="/Assets/Icons/icons8-plus-100.png"
                   Stretch="Uniform"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Width="50"
                   Height="50"/>
        </Button>
    </Grid>
</Page>
